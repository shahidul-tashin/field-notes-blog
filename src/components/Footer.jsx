export default function Footer() {
  return (
    <footer className="footer footer-center p-8 border-t border-base-300 mt-16 bg-base-100">
      <div>
        <p className="font-mono text-xs uppercase tracking-widest text-base-content/60">
          Field Notes — entries filed one at a time
        </p>
      </div>
    </footer>
  )
}
